/*
	Each CAN frame object will contain:

		CAN ID
		DLC (data length code)
		8-byte data field
		Maybe timestamp later

	We will generate CANFrame objects (either manually or by a generator function) in mainOperatorOverloading.cpp file
	
*/